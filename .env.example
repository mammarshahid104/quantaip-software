# Copy this file to .env.local and fill in your key, then restart `npm run dev`.
# WARNING: VITE_ env vars are inlined into the client bundle and are visible to
# anyone who uses the deployed app. Only use this with a trusted admin audience.
VITE_ANTHROPIC_API_KEY=sk-ant-your-key-here
